import { Context, RelationField } from "../../types";
import { Node } from "../../classes";
import { CypherStatement } from "../types";
declare type TargetNode = {
    varName: string;
    node?: Node;
    parameters?: Record<string, any>;
};
declare type TargetRelation = {
    varName: string;
    relationField: RelationField;
    parameters?: Record<string, any>;
};
export declare function buildRelationStatement({ leftNode, rightNode, context, relation, }: {
    leftNode: TargetNode;
    rightNode: TargetNode;
    relation: TargetRelation;
    context: Context;
}): CypherStatement;
export {};
//# sourceMappingURL=build-relation-statement.d.ts.map