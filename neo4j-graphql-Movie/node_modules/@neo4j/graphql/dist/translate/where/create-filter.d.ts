export declare enum Operator {
    INCLUDES = "IN",
    IN = "IN",
    MATCHES = "=~",
    CONTAINS = "CONTAINS",
    STARTS_WITH = "STARTS WITH",
    ENDS_WITH = "ENDS WITH",
    LT = "<",
    GT = ">",
    GTE = ">=",
    LTE = "<=",
    DISTANCE = "="
}
declare function createFilter({ left, operator, right, not, }: {
    left: string;
    operator: string;
    right: string;
    not?: boolean;
}): string;
export default createFilter;
//# sourceMappingURL=create-filter.d.ts.map